<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
<!--    spring配置文件 idea 为我们提供了模板-->
<!--    文件名不一定是spring.xml 可以是其他的名-->
<!--    这个文件做好放在类路径中，方便后期移植-->
<!--    放在resoruces根目录下，就相当于是放到了类的根路径下-->
<!--    配置bean 这样spring才能帮助我们管理这个对象-->


    <!--
        bean标签的两个重要属性：
            id：是这个bean的身份证号码，不能重复，是唯一标识
            class：必须填写类的全路径，全限定名类（带包名的类名）
    -->
    <bean id="userBean" class="org.example.spring6.bean.User"/>
    <bean id="userDaoBean" class="org.example.spring6.dao.UserDaoImplForMySQL"/>
    <bean id="nowTime" class="java.util.Date"/>

</beans>

